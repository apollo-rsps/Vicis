package rs.emulate.scene3d.backend.opengl.bindings

/**
 * A collection of shader types supported by the backend.
 */
//@todo - this is really backend agnostic, and there are more shader types
enum class OpenGLShaderModuleType {
    VERT,
    FRAG
}
