package rs.emulate.legacy.widget.type

/**
 * A pair of colours, one of them the default, and one the secondary.
 *
 * @param default The default colour.
 * @param secondary The secondary colour.
 */
class ColourPair(val default: Int, val secondary: Int)
