package rs.emulate.legacy

/**
 * A class which points to a file in the cache.
 *
 * @param type The file type.
 * @param file The file id.
 */
data class FileDescriptor(val type: Int, val file: Int)
