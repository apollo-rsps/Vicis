package rs.emulate.editor.workspace.resource.bundles.legacy.config

import rs.emulate.editor.workspace.resource.ResourceId

data class ObjectResourceId(val id: Int) : ResourceId

data class ItemResourceId(val id: Int) : ResourceId

data class NpcResourceId(val id: Int) : ResourceId
